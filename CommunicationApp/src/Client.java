    // create connection to server
    // if connected
    // show loginUI
    //    send login message
    //      if (LoginMessage.getStatus == Success) // good login
    //          load gui - New ClientGUI(Client, LoginMessage.getCurrentUser())
    //          spawn new thread to listen for incoming messages
    //      else 
    //          reprompt login


public class Client {

    public static void main(String[] args) {
        // Make connection to server
    }
    // Lister thread will execute this method and wait for incoming messages
    // as messages come through
    public void listenForMessage() {

    }
}
